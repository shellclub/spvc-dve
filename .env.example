# คัดลอกเป็น .env บนเซิร์ฟเวอร์ (สำหรับ docker-compose)
# และสร้าง .env.production สำหรับแอป (AUTH_SECRET, NEXTAUTH_URL)

# MySQL (ใช้โดย docker-compose)
MYSQL_ROOT_PASSWORD=ตั้งรหัสผ่าน_root
MYSQL_DATABASE=spvc_dve
MYSQL_USER=spvc
MYSQL_PASSWORD=ตั้งรหัสผ่าน_user
